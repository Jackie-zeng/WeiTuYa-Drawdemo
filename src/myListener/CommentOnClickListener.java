package myListener;

import android.view.View;
import android.view.View.OnClickListener;

public class CommentOnClickListener implements OnClickListener{

	@Override
	public void onClick(View arg0) {
		// TODO 自动生成的方法存根
		/*
		Intent intent = new Intent(MainActivity.this, Comment_view.class);
		Bundle bundle = new Bundle();
		if(initpic != null && initpicurl != null)
	  {
		bundle.putString("initpic", initpic);
		bundle.putString("tuyapic", "0");
		bundle.putString("url", initpicurl);
		intent.putExtras(bundle);
		
		societysetWindow.dismiss();
		startActivity(intent);
	  }
		*/
	}

}
